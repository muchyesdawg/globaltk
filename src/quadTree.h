#include "head.h"
namespace MDataTypes{
    inline namespace quadTreeNode{
        class node{
            public:
                void* data;
                node *e,*s,*w,*n;
        };
        extern node *t;
    }
    class quadTree{

    };
}
